# 仕様書 — soracodelab (ポートフォリオサイト リニューアル版)

## 1. ディレクトリ構成

```
soracodelab/
├─ index.html
├─ package.json / vite.config.ts / tsconfig*.json / eslint.config.js
├─ public/
│  └─ favicon.svg
└─ src/
   ├─ main.tsx
   ├─ index.css
   ├─ App.tsx
   ├─ data/            # 表示データ (コンテンツ更新はここだけを触る)
   │  ├─ works.ts
   │  └─ skills.ts
   ├─ lib/
   │  └─ motion.ts     # framer-motion の共通 variants
   ├─ hooks/
   │  └─ useMediaQuery.ts
   └─ components/
      ├─ Background.tsx
      ├─ Navbar.tsx
      ├─ Hero.tsx
      ├─ About.tsx
      ├─ Skills.tsx
      ├─ Works.tsx
      ├─ WorkDetailModal.tsx
      ├─ Footer.tsx
      └─ icons/GithubIcon.tsx
```

## 2. ページ構成 / セクション

| # | セクション | id | 内容 |
|---|---|---|---|
| 1 | Navbar | - | 固定ヘッダー。ロゴ + Home/About/Skills/Works/Contact アンカーリンク。スクロール量に応じてガラス背景をフェードイン。現在ビューポート内のセクションをハイライト。モバイル幅ではハンバーガーメニュー |
| 2 | Hero | `#home` | キャッチコピー、サブタイトル、紹介文、CTA（Works へスクロール）、実績サムネイルのボード、スクロール誘導 |
| 3 | About | `#about` | 自己紹介文、パッションバッジ（課題解決/自動化）、詳細カード |
| 4 | Skills | `#skills` | 4カテゴリ（Languages / Frontend & Frameworks / Engines & Tools / Specialties）のスキルカード |
| 5 | Works | `#works` | カテゴリタブ（All/Web/Extension/Desktop/Game/Mod）+ 実績カードグリッド（3Dチルト演出） |
| 6 | Footer/Contact | `#contact` | 連絡先（mailto:setuya.work@gmail.com）、GitHub・NexusModsリンク |

## 3. データ構造

### Works（`src/data/works.ts` で定義）

```ts
interface WorkItem {
  title: string;
  category: 'Web' | 'Extension' | 'Desktop' | 'Game' | 'Mod';
  categoryLabel: string;
  description: string;
  tech: string[];
  slug: string;         // #works/<slug> と画像ディレクトリ名に使う識別子
  highlight?: string;   // 公開実績・利用規模など特筆すべき成果を1行で
  details?: string;     // 詳細モーダルにのみ表示する補足説明
  stats?: { label: string; value: string }[];  // 出典が確認できる数値実績のみ
  statsAsOf?: string;   // stats の取得時点 (数値は変動するため明示)
  screenshots?: { src: string; alt: string; caption?: string }[];
  github?: string;
  extraLinks?: { label: string; url: string }[];
}
```

`highlight` が設定された実績のみ、カード内に Award アイコン付きのバッジを表示する。

既存版の7実績（サークル活動管理プラットフォーム、manaba Downloader、Transelation_tool、AttendanceOpener、CloverPit Mod、project-echo、ice_clicker）をそのまま踏襲し、`category` フィールドを新設してフィルタ用に分類する。

### Skills（`src/data/skills.ts` で定義）

既存版と同一の4カテゴリ・スキルタグ構成を踏襲。アイコンは lucide-react（`Terminal`, `Layout`, `Wrench`, `Brain`）を使用。

## 4. インタラクション仕様

- **Navbar**: `IntersectionObserver` で各セクションの可視状態を監視し、対応するナビ項目に `active` クラスを付与
- **カスタムカーソル**: 廃止した。ネイティブカーソルを隠す一方でホバー時の反応を持たず、「押せる場所」を示すポインタ形状が失われるだけだったため
- **アニメーション**: `<MotionConfig reducedMotion="user">` で全体を包み、`prefers-reduced-motion` を framer-motion 全体へ適用する。`index.css` のメディアクエリは CSS アニメーションにしか効かず、JS で style を書き換える framer-motion には作用しないため、この指定が必須
- **登場アニメーション**: `whileInView` は `once: true`。スクロールで往復するたびに再生されると煩わしいため
- **Works フィルタ**: カテゴリタブクリックで `worksData` を `category` でフィルタし、`AnimatePresence` でカード出し入れをアニメーション
- **スクロール**: スクロールスナップは廃止した。`mandatory` / `proximity` いずれでも、1画面未満のスクロール時にスナップ補正が元位置へ引き戻し「スクロールしたのに戻る」挙動になっていたため（実測: mandatory では補正イベント117回・収束7.0秒・最終位置0px）。セクション間の移動はナビの `scrollIntoView({ behavior: 'smooth' })` が担う
- **Works 詳細モーダル**: カード本体クリック、またはフッターの「詳細」ボタンで `WorkDetailModal` を開く。Esc / 背景クリック / 閉じるボタンで閉じ、Tab はモーダル内にトラップ、閉じたら元の要素へフォーカスを戻す
- **個別URL**: モーダルを開くと `#works/<slug>` を `pushState` する。この URL を直接開けば該当実績のモーダルが開いた状態で表示され、ブラウザの戻るボタンでも閉じられる（共有リンクで来た場合は基点の履歴を1つ作り、戻るでサイト外に出ないようにする）
- **スクリーンショット**: `screenshots` が設定された実績のみモーダル内にギャラリーを表示。1枚なら横幅いっぱい、2枚以上は自動で段組み。クリックで等倍のライトボックスを開き、Esc は「ライトボックス → モーダル」の順に1段ずつ閉じる。画像は `loading="lazy"` でモーダルを開くまで読み込まない。追加手順は `public/works/README.md` を参照

## 4.5 レイアウト上の既知の制約と対処

| 項目 | 対処 |
|---|---|
| アンカー遷移で見出しが固定ナビに隠れる | `.snap-section-long` に `padding-top: var(--nav-height)` を設定。`#skills` 単体の `scroll-margin-top` はスクロールスナップに上書きされるため無効 |
| 日本語の禁則処理 | `body` に `line-break: strict`、見出し類に `word-break: auto-phrase`（Chrome 119+。非対応ブラウザは通常の折り返しにフォールバック） |
| ヒーロー見出しの「〜に。」泣き別れ | 最長行の必要幅は font-size の約 11.8 倍、hero-content 幅は container の約 51.5%。この比から `clamp(1.65rem, 4vw, 3rem)` とし、375px〜1440px で2行に収まることを実測確認済み |
| Skills カード4枚が3カラムで1枚孤立 | 641px〜1150px は `grid-template-columns: repeat(2, 1fr)` で2x2に固定 |
| 詳細モーダルの幅 | スクリーンショットを載せるため `min(880px, 100%)`。実測で 854px、横方向のはみ出しなし |
| Works カードの下部余白 | 固定 `height: 380px` をやめ `min-height: 320px` + `height: 100%` とし、グリッドの行揃えに委ねる |
| カードのホバー色 | `nth-child(even)` だとフィルタ切替で同じカードの色が変わるため、`data-category` 属性で決める |
| ヒーローがナビに潜り込む | `.snap-section` は `align-items: safe center`。`safe` がないと内容が画面高を超えたとき上下両方向にはみ出し、上端がナビの下に入る |
| SCROLL 表示が本文と重なる | 1カラム幅では非表示。ただし `.scroll-indicator` の基本定義**より後ろ**にメディアクエリを置くこと（同詳細度で後勝ちのため、前に置くと打ち消される） |
| スクロールが引っかかる | スクロールスナップを廃止（上記「インタラクション仕様」参照） |

## 5. アクセシビリティ

- `<a href="#main-content">` の skip-to-content リンクを設置
- 見出しは `h1 > h2 > h3` の階層を崩さない
- インタラクティブ要素に `:focus-visible` スタイルを適用
- 画像/SVGイラストには適切な `role` / `aria-hidden` を設定（装飾目的のため `aria-hidden="true"`）

## 6. デザイントークン（`index.css`）

既存版の CSS 変数を踏襲しつつ整理:

```css
--primary: #f78ac6;
--secondary: #00a8f3;
--bg: #060913;
--card-bg: rgba(22, 31, 48, 0.4);
--border: rgba(247, 138, 198, 0.2);
--text: #e2e8f0;
--text-muted: #94a3b8;
--font-display: 'Space Grotesk', sans-serif;
--font-sans: 'Outfit', sans-serif;
```

## 7. 連絡先情報

- Email: `setuya.work@gmail.com`
- GitHub: `https://github.com/SetuyaSora`
- NexusMods: 既存版のプロフィールリンクを踏襲
